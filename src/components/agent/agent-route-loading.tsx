import { DashboardRouteLoading } from '@/components/dashboard/dashboard-route-loading';

export function AgentRouteLoading({ className }: { className?: string }) {
  return <DashboardRouteLoading className={className} />;
}
